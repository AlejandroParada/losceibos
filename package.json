{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "vuepress": "^1.3.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vuepress dev pags",
    "build": "vuepress build pags",
    "deploy": "npm run build && git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC"
}
